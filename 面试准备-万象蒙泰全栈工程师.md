# 万象蒙泰 · 全栈工程师（熟悉 AI）面试准备（详细版）

> 岗位：成都万象蒙泰教育 · 全栈工程师（熟悉 AI）· 8-13K·13 薪 · 华阳/麓湖  
> 结合 JD（教育产品 / Java·Spring·Node·Python / Vue·React·小程序）与简历（杨琳：AI Agent、Vue3+Node、小程序、活动配置）定制。  
> **回答节奏**：先给结论（10 秒）→ 讲链路/方案（60–90 秒）→ 举简历项目（30 秒）→ 落到教育场景（20 秒）→ 停顿等追问。

---

## 开场：30 秒自我介绍（建议背熟）

面试官你好，我叫杨琳，软件工程本科，7 年多全栈开发经验，目前在成都，已离职可尽快到岗。

前几年主要做游戏社区和商城：用 Flutter 做多端社区，用 Vue3 + Node.js 做管理后台和活动/商城配置，商城上线三年创收近千万，付费转化大约 25%。从 2024 起我把重心转到 AI 辅助研发，独立做过「AI 游戏活动配置 Agent」：运营用自然语言描述活动，系统自动完成需求结构化、素材匹配、UI 预览和 H5 生成，技术上用到 DeepSeek、Prompt 工程、Zod 校验、MCP 工具编排。

我看咱们岗位是教育产品的全栈，又强调熟悉 AI，我觉得我的路径很契合——把「配置化后台」升级成「AI 自动化配置」，同时前后端都能独立交付。今天可以重点聊 Agent 落地、Node/Vue 后台，以及小程序相关经验。

---

# 一、AI 相关（5 题 · 详细版）

## Q1：你说做过「AI 活动配置 Agent」，整体链路怎么跑通？LLM 不稳定时怎么兜底？

### 这题在考什么
有没有真实落地过 Agent（不是只会调 Chat API）；是否懂「结构化输出 + 校验 + 工具编排」；能否把经验迁移到教育产品。

### 完整参考回答（约 2 分钟）

**结论先说：**  
我做的是游戏运营场景的 AI 活动创建工具链，目标是把传统「口述需求 + 后台手工配表」压缩成单人自助流程。技术栈是 Vue3 + Express + DeepSeek API + MCP（墨刀）+ Zod + Prompt Engineering。闭环是：**自然语言 → 结构化活动配置 → UI 预览 → H5 产出 → 可对接原有 MySQL 活动表**。

**链路分三层：**

1. **理解层（LLM 结构化）**  
   运营在向导第一步用自然语言描述，比如「下周新游上线，做个三日签到，奖池用某某道具」。System Prompt 规定角色与输出字段；输出按 JSON Schema 契约；同时动态注入当前游戏的 registry、素材 catalog（轻量 RAG），避免模型凭空编素材 ID。

2. **校验层（服务端闸门）**  
   DeepSeek 开 JSON mode 拿到结构化结果后，**服务端再用 Zod 校验一遍**——不过 Schema 绝不进入下游。素材 ID、活动类型走白名单；theme / colors 这类易混淆字段专门加了负向约束和 Correction Prompt。校验失败会把错误信息回灌给模型纠错，或回退让人在结构化表单里改，而不是让运营重说一遍自然语言。

3. **表现层（MCP 工具链）**  
   理解层和表现层职责分离：LLM 不负责直接吐整页不可控 HTML。后端 Orchestrator 编排 MCP 工具，例如墨刀 `gen_html` → `import_html`，生成预览；前端 Vue3 三步向导（描述 / 预览 / 发布）用 iframe 看效果，确认后再发布。Prompt 模板独立模块维护，支持 Provider 切换（modao / stitch）。

**兜底策略我会强调三点：**  
Schema 硬拦截、纠错闭环、人工可编辑结构化配置继续走原发布流。活动类型已支持折扣 / 签到 / 抽奖 / 新游宣传，配置可对接原先商城/活动后台的 MySQL 表——这是从「配置化活动引擎」演进到「AI 自动填配置」的延续，不是另起一套数据。

**落到教育：**  
「自然语言生成作业规则 / 试卷结构 / 课程活动页」可以同一套骨架：理解层出结构化草稿 → 校验闸门 → 教师预览确认 → 写入正式表。核心不是模型多聪明，而是**契约、校验、编排、人机确认**。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 为什么不用让模型直接生成 HTML？ | HTML 不可校验、难对接原有活动表、难复用运营规则；结构化配置可入库、可人工改、可对接老引擎。 |
| RAG 怎么做的，有向量库吗？ | 一期是轻量做法：按游戏注入 registry/catalog 子集，不是完整向量检索；教育一期也可先按教材/知识点枚举注入，真要语义检索再上向量库。 |
| 一次成功率大概多少？ | 坦诚：和活动类型复杂度、素材库完整度强相关；工程上用「校验通过率 + 人工修改率」衡量，比吹通过率更专业。 |
| Orchestrator 怎么实现？ | Express 里编排：parse → validate →（可选 correct）→ mcp gen → preview URL；每步落日志，失败可断点续跑。 |

### 关键词（面试官爱听）
结构化输出、JSON Schema、Zod、白名单、负向约束、Correction、MCP、职责分离、人机协同、草稿态发布。

---

## Q2：Prompt Engineering 你具体怎么设计？有没有踩过坑？

### 这题在考什么
是否把 Prompt 当工程问题；有没有迭代方法；能否控制输出稳定性。

### 完整参考回答

**结论：**  
我把 Prompt 当成**可版本管理的产品模块**，和业务代码解耦，而不是写死在某个 controller 里的超长字符串。活动解析有独立的 System Prompt + 输出契约，支持按 Provider/场景切换。

**设计分五块：**

1. **角色与边界**  
   「你是活动配置助手；只输出约定字段；禁止编造不存在的素材 ID；不确定就留空或标需确认，不要猜。」

2. **输出契约**  
   每个字段写清含义、类型、枚举（活动类型、状态字段等）。和前端表单 Schema、后端 Zod Schema 尽量同源，避免「Prompt 一套、校验一套、表单一套」。

3. **动态上下文（轻量 RAG）**  
   按当前所选游戏注入：可用素材列表、已有活动模板、主题枚举。只注入当前需要的子集，避免上下文噪声。

4. **负向约束（我实际踩坑后加的）**  
   例如 theme 与 colors 易混：明确 theme 是风格标签，colors 是色值数组，禁止互相填充；禁止在 JSON 外输出解释文字。

5. **纠错闭环**  
   Zod 失败时，把 `issues`（缺字段、类型错、ID 不在白名单）格式化后塞进 Correction Prompt，让模型只修错误字段，而不是全量重生成。

**踩过的坑（建议主动讲，显成熟）：**

- **坑 1**：早期只写「请输出 JSON」，模型偶发夹 Markdown 代码块或解释文字 → 改 DeepSeek JSON mode + 服务端 Zod，稳定性明显上升。  
- **坑 2**：把整个素材库塞进 Prompt，又贵又干扰 → 改为按游戏/类型过滤的 catalog。  
- **坑 3**：一次 Prompt 既要理解需求又要生成精美 UI → 拆成理解层与表现层，UI 交给 MCP 工具。  
- **坑 4**：没有版本管理，改 Prompt 影响所有活动类型 → 模板模块化，按活动类型可覆盖局部规则。

**教育场景映射：**  
题型、年级、学科、知识点做成枚举与白名单；Prompt 只负责理解教师意图；分值、截止时间、是否允许补交等业务规则由服务端 Schema 裁决。教师侧保留「改草稿」入口——教育产品对正确性更敏感，人机协同是标配。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| temperature 怎么设？ | 结构化抽取偏低（偏确定性）；文案/主题描述可略高；校验严格时优先稳。 |
| 如何评测 Prompt 好坏？ | 固定用例集（签到/折扣/抽奖各若干条）+ 校验通过率 + 关键字段准确率 + 人工修改次数。 |
| few-shot 用不用？ | 复杂枚举或易混字段会加 1–2 个正例/反例；不堆很多，怕挤占动态 catalog。 |
| 和 LangChain 关系？ | 我更偏显式 Orchestrator + 自管 Prompt 模块；需要时可用框架，但核心仍是契约与校验。 |

---

## Q3：什么是 MCP？项目里怎么用？和 Function Calling 有何区别？

### 这题在考什么
对 Agent 工具生态的理解；是否真接过外部工具；架构抽象能力。

### 完整参考回答

**结论：**  
MCP（Model Context Protocol）可以理解为：**让 Agent 以标准协议接入外部工具和资源的一层约定**。模型不只是聊天，还能按工具描述去调用外部能力（生成页面、读资源、写回设计稿等）。

**我在项目里的用法：**  
理解层先产出「活动结构化配置」，再由后端 Orchestrator 作为 MCP Client，调用墨刀侧工具（如 `gen_html`），再 `import_html` 进入可预览链路；也探索过 Stitch UI Agent。关键点是：**LLM 不直接对生产 HTML 负责**，工具输入是校验后的结构化数据，输出进入预览，人确认后再发布。

**和 Function Calling 对比（面试可画表）：**

| 维度 | Function Calling | MCP |
|------|------------------|-----|
| 形态 | 应用内定义函数/工具给模型调 | 协议化的 tools/resources，可跨产品 |
| 耦合 | 常绑在某一厂商 SDK/会话里 | 更像「工具服务器」可插拔 |
| 适用 | 单应用内查库、下单、检索 | 多工具链、换 Provider、生态接入 |
| 我的选择 | 简单内部动作也可用 FC | UI 生成等外部能力用 MCP，便于 modao/stitch 切换 |

**工程细节可补充：**  
- 封装 MCP Client，统一超时、重试、错误码；  
- Prompt/工具描述独立维护；  
- Provider 切换时业务编排代码尽量不动；  
- 工具调用全程打日志（入参摘要、耗时、成功失败），方便排障和算成本。

**教育场景：**  
可把「题库检索」「课件模板渲染」「作文批改服务」「TTS 朗读」做成工具；Agent 负责编排「出题 → 组卷 → 预览」，而不是把所有能力写进一个超级 Prompt。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 工具调用失败怎么办？ | 重试有限次 → 降级到仅保存结构化配置、跳过自动 UI → 提示人工用原后台改。 |
| 会不会让模型自己决定调哪个工具？ | 关键路径我偏 Orchestrator 显式编排，可控可测；探索性任务再给模型选工具。 |
| MCP 和你们业务服务器关系？ | 业务 API 仍是 Express；MCP 是旁路能力，通过 Orchestrator 接入，不污染主交易链路。 |

---

## Q4：如何避免 LLM 幻觉写入业务数据？教育场景怎么做？

### 这题在考什么
风险意识；是否懂「模型不可信、系统要可信」；教育数据敏感度。

### 完整参考回答

**原则一句话：模型只生成建议/草稿，权威数据以系统与数据库为准。**

**我落地过的五道防线：**

1. **Structured Output（JSON mode）** — 先锁结构，减少自由文本。  
2. **服务端 Schema 二次校验（Zod）** — 类型、必填、枚举、范围；失败即拒绝。  
3. **白名单** — 素材 ID、活动类型、关联商品 ID 必须存在于库/catalog；幻觉 ID 直接拦截。  
4. **人机确认与状态机** — 预览（iframe）→ 用户点发布才入库/对 C 端可见；草稿与正式分离。  
5. **审计日志** — 存原始自然语言、模型原始输出、校验结果、最终快照，出问题可回放。

**额外工程手段：**  
幂等发布、权限校验（谁能发哪条业务线）、配置变更 diff（发布前看改了哪些字段）。

**教育场景要加强：**

| 数据类型 | 策略 |
|----------|------|
| 题库题目/知识点 ID | 只能从检索结果选，禁止 invent |
| 成绩、学籍 | AI 不直写正式成绩表；批改结果进待确认 |
| 教案/作业 | 草稿态，教师确认后发布到班级 |
| 面向学生的内容 | 只下发「已发布版本」，不下发生成中草稿 |
| 隐私 | Prompt 尽量不塞明文敏感个人信息；脱敏/最小化 |

**话术收尾：**  
LLM 负责理解与生成草稿，数据库、权限、发布流负责真相。教育产品宁可少自动化半步，也要保证可审计、可撤回。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 全自动可以吗？ | 内部运营工具可逐步提高自动比例；对学生/成绩相关默认人工确认。 |
| 幻觉仍然发生怎么办？ | 拦截率、误放行要监控；坏例加入评测集反哺 Prompt/规则。 |
| 和内容安全关系？ | 生成内容可接敏感词/学科合规策略；发布前再扫一遍。 |

---

## Q5：若把 AI 接到教育产品（作业/答疑/内容生产），一期 MVP 怎么设计？

### 这题在考什么
产品sense、边界意识、能否复用已有方法论、是否会一上来造大而全平台。

### 完整参考回答

**结论：**  
一期只做一个**闭环短、可量化、教师愿意用**的场景。我建议：**教师自然语言描述 → 生成结构化作业/练习配置 → 预览 → 发布到班级**。这和我做的活动配置 Agent 同构，迁移成本低。

**为什么选这个而不是先做答疑机器人？**  
- 答疑要面对胡问、知识正确性、课标对齐，风险高、评测难；  
- 作业配置有明确 Schema，和后台表单本就存在，AI 是「加速填表」；  
- 成功标准清晰：配置耗时、一次通过率、教师修改率、发布成功率。

**MVP 架构：**

```
教师 Web（Vue3 向导）
  → API：parse / correct / preview / publish
  → Prompt 模块 + LLM Provider（DeepSeek 等，可切换）
  → Schema/Zod 校验
  →（可选）题库检索 RAG：按年级学科知识点取题
  → 草稿表 → 教师确认 → 正式作业表
学生小程序：只拉「已发布」作业
```

**角色分工：**  
- 前端：三步向导，降低学习成本；支持改结构化结果再生成预览。  
- 后端：Orchestrator；鉴权（学校/班级数据范围）；审计。  
- AI：可独立服务，主站 Java 也行，AI 用 Node/Python，HTTP 对接。

**明确不做（显示你稳）：**  
一期不上全自动批改入成绩、不上多 Agent 大而全、不上面向学生的自由闲聊答疑。先把「人机协同配置效率」跑通，再扩：组卷、个性化练习、课后答疑（检索增强 + 引用教材片段）。

**成功指标（建议主动提）：**  
教师从进页到发布的中位耗时；AI 草稿到最终稿的字段修改率；发布成功率；单位作业的模型成本。

**和简历的衔接句：**  
我过去从「配置化活动引擎」做到「AI 自动填配置」，教育侧我会先摸清你们现有作业/课程表结构，把 AI 接在「填配置」而不是「另起一套」，这样上线快、和现有后台兼容。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 多久能出一期？ | 若已有作业模型和后台，优先接 AI 解析+草稿+预览；具体取决于现有接口与权限复杂度，我会先做技术spike再给排期。 |
| 用哪家模型？ | 先国产/DeepSeek 控成本与合规，抽象 Provider，关键链路可换更强模型。 |
| 如何防教唆/超纲？ | 系统提示约束学段；题库检索优先；发布前教师确认；可加简单规则校验（年级-知识点匹配）。 |

---

# 二、后端相关（5 题 · 详细版）

## Q1：你主导的后台「日均百万级请求」，架构和优化怎么做的？

### 这题在考什么
是否理解读多写少、缓存、隔离、索引；数字是否经得起追问（注意：百万级主要来自 C 端/SDK 读流量，管理后台本身通常没那么高）。

### 完整参考回答

**先澄清口径（显专业）：**  
「日均百万级」主要指游戏 SDK / 社区 / 活动 / 商城相关接口的整体访问量级，特征是**读多写少**：C 端大量读配置与商品，运营后台是低频写配置。我负责的是 Vue3 + Node.js（Express）后台与相关 API，对接 MySQL，支撑多游戏、多业务线，稳定运行两年以上。

**我做的几件事：**

1. **读写与职责隔离**  
   管理端写操作（配活动、改商品）与 C 端读配置接口分离，避免运营批量保存拖垮读链路；发布才刷新对 C 端可见的数据。

2. **缓存热点**  
   活动配置、商品信息、部分 Schema/字典走缓存（Redis 或本地+过期）；配置变更时主动失效或版本号升级，防止长期脏读。

3. **数据库**  
   活动/商品按业务键、状态、时间建索引；列表强制分页；接口字段裁剪，避免 select *；大JSON 配置注意更新粒度。

4. **接口治理**  
   超时、限流、统一错误码；关键路径日志与监控；避免同步级联调用过深。

5. **配置化活动引擎**  
   活动/商品/运营模块标准化后，新活动多为配表而非发版，减少变更风险，也是后来 AI Agent 能「填同一套 Schema」的前提。

**教育映射：**  
课前课后、考试周会有峰值；课程配置、作业列表同样读多写少——缓存已发布配置、管理端与学生端隔离、按学校/班级维度索引，思路可复用。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| Node 单线程怎么扛？ | 集群多进程/多实例 + 反向代理；CPU 密集少做；IO 型适合；必要时把重任务异步化。 |
| 有没有分库分表？ | 按当时体量以索引+缓存+分页为主；若单表过大再按业务线/时间归档。如实说，不装。 |
| 缓存一致性？ | 先更新 DB 再删缓存，或发布时 bump 版本号；接受极短窗口，业务可容忍。 |
| Redis 挂了怎么办？ | 降级打 DB + 限流；核心配置可本地短缓存兜底。 |

---

## Q2：REST API 怎么设计才可维护？参数校验怎么做？

### 完整参考回答

**结论：**  
可维护的 API = 清晰资源模型 + 统一约定 + 入口强校验 + 后端鉴权，而不是只靠前端防呆。

**我的习惯：**

1. **资源化**  
   `/activities`、`/products`、`/orders`；用 GET/POST/PUT/PATCH/DELETE 表达动作；嵌套适度（`/games/:id/activities`），避免过深。

2. **统一响应**  
   `{ code, message, data, requestId? }`；业务错误与系统错误区分；前端可统一拦截。

3. **版本/模块前缀**  
   多业务线共存时用前缀或版本，避免一年后字段混乱互相踩踏。

4. **Zod（或同类）入口校验**  
   body/query/params 进业务前校验；失败直接 400，带字段级错误，方便联调。AI 项目里同一套 Zod 也校验 LLM 输出——**人机接口同一契约思维**。

5. **鉴权与 RBAC**  
   登录态（Token/Session）；角色→菜单/按钮/接口；动态路由只解决「看得见」，**接口层必须再鉴权**。写操作校验数据归属（只能改自己业务线/游戏）。

**若团队是 Java/Spring Boot：**  
Controller → DTO + Bean Validation（`@Valid`）→ Service → Mapper，和 Express 路由→校验→Service→DAO 一一对应。我可以按团队规范写，思想不冲突。

**教育注意点：**  
列表接口默认带组织范围（学校/院系/班级）；禁止只靠前端藏按钮；导出类接口单独鉴权与审计。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| PUT 和 PATCH？ | PUT 偏完整更新，PATCH 偏部分字段；团队统一即可。 |
| 幂等？ | 创建类可用幂等键；支付/领券必须幂等，防重复点击。 |
| 文档？ | OpenAPI/Swagger 或至少类型共享；前后端契约变更要通知。 |

---

## Q3：MySQL 活动/商品表怎么设计？如何配合配置化活动引擎？

### 完整参考回答

**设计原则：稳定字段进列，易变规则进 JSON/配置表，可检索关系进关联表。**

**表结构思路：**

1. **活动主表**  
   `id, game_id/biz_line, type, title, status, start_at, end_at, created_by, created_at, updated_at, published_at…`  
   status：草稿 / 待发布 / 进行中 / 已结束 / 下线。

2. **配置体**  
   `config_json`（或 `activity_config` 表）：折扣规则、签到天数、奖池、主题色等。  
   好处：新活动类型加字段不一定改表结构；坏处：复杂查询弱——所以需要检索的条件仍要抽列或关联表。

3. **商品/奖品关联表**  
   `activity_id + product_id/sku_id + extra`，多对多，避免只在 JSON 里写死导致无法统计、无法校验商品是否存在。

4. **发布与版本（可选增强）**  
   草稿改动不影响线上；发布生成版本快照，出问题可回滚。

**配置化引擎怎么配合：**  
后台用 **Schema 驱动表单渲染**（不同类型活动不同表单）；运营改配置不发版。我后来用 AI 辅助生成活动表单 Schema 与渲染逻辑，迭代效率大约提升 30%。AI Agent 本质上是**自动填写同一份 Schema**，最终仍写入同一套 MySQL——所以老后台、新 Agent、C 端消费三方对齐。

**教育类比：**

| 游戏活动 | 教育作业 |
|----------|----------|
| 活动主表 | 作业主表 |
| config_json（规则） | 限时、重做次数、显示解析时机 |
| 商品关联 | 题目关联（题库 ID） |
| 发布到游戏 | 发布到班级 |
| AI 填活动 Schema | AI 填作业 Schema |

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| JSON 字段要不要拆？ | 常查、常筛、常统计的拆列；纯展示/规则细节可 JSON。 |
| 事务？ | 发布时主表状态 + 关联表 + 缓存失效放同一事务或用出资询保证最终一致。 |
| 软删除？ | 运营配置类常用软删+状态，便于审计。 |

---

## Q4：SSE 用在什么场景？和 WebSocket 怎么选？

### 完整参考回答

**SSE（Server-Sent Events）：**  
HTTP 长连接，**服务端→客户端单向**推流。我用在 AI 生成场景：把 LLM token 流式推到前端做打字机效果，或推送 Orchestrator 步骤进度（解析中 / 校验中 / 生成预览中）。实现相对简单，走标准 HTTP，多数代理友好。

**WebSocket：**  
全双工。适合 IM、协作编辑、在线教室师生互动、实时白板等**需要客户端频繁上行**的场景。

**选型口诀：**  
- 生成式 AI 输出、进度日志 → **SSE 优先**  
- 课堂实时互动、多人同步 → **WebSocket**  
- 普通 CRUD → 短请求即可，别上推送

**工程注意（建议主动提）：**

1. 客户端断开时，服务端要 **cancel 上游 LLM 请求**，避免空耗配额。  
2. 超时与心跳：代理可能切断空闲连接，需注释或定期 ping（SSE 注释行）。  
3. 错误事件：流中推 `event: error`，前端可重试或降级为非流式一次返回。  
4. Node 注意连接数与内存；必要时生成服务与主 API 分离。  
5. 鉴权：SSE 也要带 Token；注意某些环境 GET 头限制，可用 query 短票（注意安全）。

**教育：**  
教案/作业生成、智能批改说明用 SSE；直播课互动用 WS。不要把所有实时需求都上 WS，增加运维复杂度。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 浏览器兼容？ | 现代浏览器支持 SSE；小程序端不一定原生 SSE，可用分块/轮询/云函数通道，需按端评估。 |
| 和 fetch stream？ | 也可用 fetch 读流；SSE 是事件协议更结构化。 |

---

## Q5：JD 有 Java/Spring、Python，你主栈是 Node，怎么融入？

### 这题在考什么
学习能力、态度、是否排斥换栈；能不能在语言之外贡献架构与 AI 经验。

### 完整参考回答

**坦诚定位：**  
我近几年后端主栈是 **Node.js（Express）+ MySQL**，前端 Vue3，另有 Flutter/小程序；Python 能做落地（尤其 AI 脚本/服务）。不是长期 Java 业务开发出身，但**后端共性我熟**：REST、鉴权、事务与一致性、缓存、异步、校验、日志监控、领域建模。

**融入路径：**

1. **先学领域，再学语法**  
   课程、班级、作业、权限、组织架构——教育领域模型比语言更决定能不能交付。

2. **用熟悉范式对标**  
   Spring：Controller / Service / Mapper ≈ Express 路由 / Service / DAO；DTO + Validation ≈ Zod；Interceptor/AOP ≈ 中间件。

3. **AI 能力可独立服务化**  
   主站若是 Java，AI Orchestrator 可用 Node 或 Python 独立部署，HTTP/消息通信——我做过 Agent 与后台解耦，这条路现实且低风险。

4. **短期交付抓手**  
   配置化后台、API 设计、AI 接入与 Schema 校验、Vue 管理端、小程序联调——这些我能较快产出；Java 业务模块边做边对齐团队规范。

**态度句（重要）：**  
我不会要求团队迁就我的 Node；以你们现有技术栈为准。我的差异化是把已验证的 **AI Agent 工程化（Prompt、校验、工具编排、发布流）**带进教育产品，同时补齐 Java 业务开发细节。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 写过 Spring Boot 吗？ | 如实：不是主力项目栈；理解分层与常用组件，入职后按规范上手，可用现有模块对照学习。 |
| Python 做什么？ | AI 调用、数据处理、脚本；与 Node 类似都适合做 AI 服务层。 |
| 如何证明学习快？ | 举例：从纯前端到独立扛 Node 后台与活动引擎；再到独立落地 Agent 全栈。 |

---

# 三、Vue 与小程序相关（5 题 · 详细版）

## Q1：Vue3 管理后台怎么搭？Pinia、动态路由、权限怎么做？

### 完整参考回答

**项目背景：**  
我从 0 到 1 搭过游戏 SDK 管理后台、社区/商城管理后台：Vue3 + Pinia + Element Plus，含 RBAC、动态路由、配置化活动引擎；并用 AI 辅助生成表单 Schema，后台迭代效率大约提升 30%。

**搭建要点：**

1. **信息架构**  
   布局：侧边栏 + 顶栏 + 内容区（可多页签）。模块按业务拆：商品、活动、运营、用户权限、游戏/业务线切换。

2. **Pinia 状态**  
   - `user`：用户信息、token  
   - `permission`：角色、菜单树、按钮权限码  
   - `app`：字典、全局配置、当前业务线  
   持久化仅必要字段（如 token），权限以服务端为准，进入时刷新。

3. **动态路由**  
   登录成功 → 拉菜单/路由配置 → `router.addRoute` → 再进首页。  
   全局守卫：无 token 去登录；无权限去 403；处理刷新后动态路由丢失（重拉权限）。

4. **RBAC**  
   角色 → 菜单 + 按钮 + 接口。  
   前端：自定义指令/组件控制按钮显隐。  
   **强调：前端隐藏 ≠ 安全，后端接口必须鉴权。**

5. **工程化**  
   目录按模块；API 层统一封装；错误与 loading 统一；TypeScript 类型；Cursor Rules/Skills 约束 Vue3/TS 规范，提升 AI 辅助编码一致性。

**教育后台增量：**  
不止菜单权限，还有**数据权限**：校长/年级主任/班主任看到的班级范围不同。做法：登录后下发 dataScope；请求拦截器带组织上下文；列表 SQL/过滤按范围裁剪。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 刷新白屏？ | 动态路由未恢复：在守卫里检测未加载则先拉权限再 `next(to.fullPath)`。 |
| 按钮权限码设计？ | `activity:publish`、`product:edit` 这类资源:动作。 |
| 微前端？ | 当时单体后台足够；多团队再考虑 qiankun/Module Federation。 |

---

## Q2：Composition API 相比 Options 的优势？复杂页面怎么组织？

### 完整参考回答

**优势（结合实践说）：**

1. **按业务能力聚合** — 相关 state/方法/副作用放一起，而不是 Options 里 data/methods/watch 拆散。  
2. **逻辑复用** — `useXxx` 比 mixin 更清晰，无命名冲突、来源可追溯。  
3. **TypeScript 友好** — 类型推导更好。  
4. **与 AI 辅助编码契合** — 边界清晰的 composable 更容易生成与评审（我用 Claude Code / Cursor 辅助开发时体会深）。

**复杂页组织示例（活动 AI 三步向导）：**

- `useActivityParse`：提交自然语言、收 SSE/结果、错误态  
- `useActivitySchema`：Zod/表单模型、字段校验、Correction  
- `usePreview`：调 MCP 生成、iframe URL、刷新预览  
- `usePublish`：发布、幂等、成功跳转  

页面组件只做步骤条编排和布局；单步 UI 组件变「傻」，逻辑可单测/可复用。

**列表页经典拆法：**  
`useTable`（分页、查询、loading）+ `useDialogForm`（新建编辑）+ 业务 API。

**小程序侧：**  
思想同样适用（hooks/composables）；uni-app 里也可抽 `useAuth`、`useCouponReceive`，避免每个页复制一份领取逻辑。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| setup 里会不会太乱？ | 超 200 行就拆 composable；页面保持编排。 |
| ref 和 reactive？ | 基本类型/替换整体用 ref；结构清晰对象可用 reactive；团队统一更重要。 |
| watch 与 watchEffect？ | 明确依赖用 watch；想少写依赖可用 watchEffect，但要防多余触发。 |

---

## Q3：小程序（uni-app 等）和 Vue H5 的关键差异？

### 完整参考回答

**经历锚点：**  
豪斯莱做过优惠券小程序（uni-app）；旺小宝参与入职小程序；班图社区产品覆盖小程序端；另外还有 H5/App（后用 Flutter 统一多端）。

**关键差异：**

1. **运行时**  
   无浏览器 DOM/BOM；组件是小程序组件树；部分 Web API 不可用。性能上注意频繁 setData/大对象更新（原生思路），uni-app 也要避免无意义整页刷新。

2. **包体积与启动**  
   主包有体积上限；活动、商城、课程模块宜**分包**；首屏只留登录与核心入口，提升打开速度——教育小程序用户很敏感。

3. **登录与账号**  
   `wx.login` → code → 后端换 session/openid；常要绑定手机号或学校账号。教育场景几乎必然做「微信身份 ↔ 学校学号/教师账号」绑定与权限同步。

4. **能力与审核**  
   支付、订阅消息、分享卡片、获取手机号都受平台规范与类目限制；上线周期要预留审核。涉及付费课/教务要提前确认类目资质。

5. **多端差异**  
   uni-app 用条件编译处理微信/H5/App 差异；样式以 rpx 为主。我后来在社区用 Flutter 统一 App/多端，是另一条降本路线——面试可说「会按产品形态选 uni-app 还是 Flutter/原生」，不迷信单一方案。

6. **调试与发布**  
   开发者工具、预览码、体验版、正式版；与 H5 的 CI 发布不一样，要有版本与灰度意识。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 原生微信小程序 vs uni-app？ | 单端极致性能/深度能力偏原生；要 H5+小程序快速多端偏 uni-app。 |
| 和 Vue3 关系？ | uni-app 已支持 Vue3；注意生命周期与小程序 API 差异。 |
| 为什么社区后来用 Flutter？ | 多端统一、降低维护成本；小程序是否保留取决于渠道获客（微信生态）。 |

---

## Q4：小程序/H5 列表与表单，性能和体验注意什么？

### 完整参考回答

**列表：**

- 分页加载（上拉），避免一次拉全量作业/帖子；  
- 很长列表考虑虚拟列表或减少同屏节点；  
- 图片懒加载、合适尺寸、CDN；  
- 骨架屏/占位，减少白屏焦虑；  
- 搜索防抖；加载加锁，防并发重复请求。

**表单与交易（我商城/优惠券经验）：**

- 本地校验 + 服务端再校验；  
- 提交中 loading，按钮防抖，**服务端幂等**（领券、下单、交作业）；  
- 成功/失败态明确，可重试；  
- 支付类：以服务端查单为准，不能只信客户端回调一次。

**弱网与异常：**  
超时提示、空态、错误重试；关键配置可缓存；断网交作业要明确「未提交成功」。

**教育场景特化：**  
交作业支持草稿；截止时间倒计时与截止后禁止提交（前后端双检）；文件/图片作业注意大小与压缩；成绩未公布前不要泄露。

**和转化的关系（可提商城数据）：**  
体验细节影响转化；我们道具商城付费转化约 25%，背后是流畅浏览、清晰权益、稳定支付与会员体系——教育里则是清晰任务、低摩擦提交、及时反馈。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 如何防重复交作业？ | 前端锁 + 后端唯一约束（user+homework 版本）+ 返回已提交状态。 |
| 大图上传？ | 压缩、分片/直传 OSS、进度条、失败续传策略视体积而定。 |

---

## Q5：教师 Vue 后台 + 学生小程序，前后端职责怎么划分？

### 完整参考回答

**总原则：按角色拆端，按领域合后端；AI 只在「配置生产侧」深度介入，学生端消费已发布结果。**

**端职责：**

| 端 | 职责 | 为什么 |
|----|------|--------|
| 教师/运营 Web（Vue3） | 课程编排、作业配置、AI 辅助生成、数据看板、权限管理 | 复杂表单、大屏操作、适合向导与预览 |
| 学生/家长小程序 | 查课、收作业、提交、消息、轻互动 | 触达强、碎片时间、微信生态 |
| （可选）家长端 | 通知、学情只读 | 权限更窄 |

**后端职责：**

- 统一账号与组织权限（学校/班级/角色）；  
- 作业/课程等领域服务；  
- 发布流：草稿 → 审核/确认 → 已发布；  
- 学生端 API 只读已发布数据 + 提交类写接口；  
- AI 服务独立：Web 调用生成，**小程序不直连 LLM**（省成本、控安全、控体验）；  
- 配置类读接口缓存；提交类接口强校验与幂等。

**发布流（重点讲）：**  
教师在 Web 用 AI 生成草稿 → 预览 → 修改 → 发布 → 学生小程序拉取发布版本。  
这和我做的「活动后台配置 → H5/小程序消费」完全同构，只是领域从游戏活动换成教学活动。

**我能立刻贡献的：**  
Vue3 配置后台、Schema 化表单、Node/API 设计、小程序联调、AI 填配置 Agent——把游戏侧验证过的路径迁到教育。

### 可能追问 & 短答

| 追问 | 怎么答 |
|------|--------|
| 要不要做 App？ | 看获客与留存；有现成 Flutter 经验，但一期可用小程序验证。 |
| BFF 要不要？ | 若 Web 与小程序模型差异大，可加 BFF 裁剪字段；初期一个 API 网关 + 按端DTO 也可以。 |
| 实时课堂？ | 另开课题；与配置后台分层，别和一期 MVP 绑死。 |

---

# 四、行为 / 匹配度（建议准备）

### 为什么看教育公司的这个岗位？
我过往强项是「运营配置工具 + 全栈交付」，最近一年把 AI Agent 真正接到配置链路上。教育同样有大量结构化配置（课、作业、活动页），又强调熟悉 AI，和我的方向一致。我也在成都，期望在教育场景把 Agent 工程化做深。

### 你的优势是什么？
不是只调过 Chat API，而是独立打通过 **NL → Schema → 校验 → MCP → 预览发布**；同时有 Vue/Node/小程序长期业务经验，能从 0 到 1 扛模块。

### 缺点/不足？
Java 不是最近几年的主力业务栈；若团队以 Spring 为主，我会用独立 AI 服务快速贡献价值，同时按规范补齐 Java 业务开发。对 K12 教研细节要入职后向业务同事深入学习。

### 薪资与到岗
区间按 JD 与市场谈；已离职，可较快到岗。薪资可先问对方带宽，再结合职责谈。

---

# 五、两个 STAR 故事（详细稿 · 建议背到能脱稿）

## 故事 A：AI 游戏活动配置 Agent（主打 AI）

- **Situation**：游戏运营配活动长期依赖「口述 + 后台手工配表」，类型多（折扣/签到/抽奖等），易错、慢；我已有配置化活动引擎与 MySQL 活动表基础。  
- **Task**：做 AI 工具链，让运营自然语言描述即可得到可预览、可发布的活动配置与 H5。  
- **Action**：设计理解/校验/表现三层；DeepSeek JSON mode + Zod + 白名单；Prompt 模块化与纠错；MCP 编排墨刀生成；Vue3 三步向导；对接原活动表。  
- **Result**：打通 NL→结构化→预览→H5 闭环；与原后台配置体系延续；沉淀 Cursor Rules/Skills 与 Provider 切换能力。  
- **时长**：90–120 秒；细节留待追问。

## 故事 B：心游社区 / 商城全栈（主打业务与全栈）

- **Situation**：游戏社区需覆盖 App/小程序/H5，并做道具商城、活动、积分 VIP、海外支付。  
- **Task**：核心开发与后台负责人，保障稳定迭代与营收能力。  
- **Action**：Flutter 多端统一；Vue3+Node 管理后台；活动/商城配置引擎；接入 Google Pay / Line Pay 等；AI 辅助提效后台。  
- **Result**：稳定运行 3 年+；商城三年创收近 1000 万；付费转化约 25%；后台支撑日均百万级请求量级。  
- **衔接**：领域知识反哺 Agent 项目——先有配置引擎，才有 AI 自动配置。

---

# 六、面试当天检查清单

1. 自我介绍 30 秒流畅。  
2. 故事 A/B 各 90 秒，能画三层架构（理解/校验/表现）。  
3. 每题尽量落到「教育怎么用」。  
4. Java 问题不硬扛，讲迁移路径与 AI 服务化。  
5. 准备 2–3 个反问：  
   - 目前教育产品技术栈（Java 占比、是否已有小程序）？  
   - AI 期望是提效工具还是 C 端学员功能？  
   - 全栈在团队里更偏后台配置还是 C 端？  
   - 一期最核心的业务指标是什么？

---

# 七、可打印「速记卡」（临进门看）

**AI：** Schema 契约 · Zod 闸门 · 白名单 · Correction · MCP 编排 · 草稿发布 · 人机协同  
**后端：** 读写隔离 · 缓存失效 · 索引分页 · RBAC 双鉴权 · 主表+JSON+关联表 · SSE 流式 · AI 可独立服务  
**Vue/小程序：** 动态路由 · 数据权限 · composable 拆分 · 分包 · code 登录绑定学号 · 幂等提交 · Web 生产 / 小程序消费  

**一句话定位：**  
我是能把「配置化后台」升级成「AI Agent 自动配置」的全栈，做过真实闭环，能迁到教育作业/课程场景。
