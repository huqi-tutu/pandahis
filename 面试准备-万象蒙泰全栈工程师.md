# 万象蒙泰 · 全栈工程师（熟悉 AI）面试准备

> 结合岗位 JD（教育产品 / Java·Node·Python / Vue·小程序）与个人简历（AI Agent、Vue3+Node、小程序、活动配置）定制。  
> 回答原则：**先结论 → 结合经历举例 → 落到教育场景可复用点**。

---

## 一、AI 相关（5 题）

### Q1：你说做过「AI 活动配置 Agent」，整体链路是怎么跑通的？LLM 输出不可靠时怎么兜底？

**参考回答：**

我做的是游戏运营场景里「自然语言 → 结构化活动配置 → UI 预览 → H5 产出」的闭环，技术栈是 Vue3 + Express + DeepSeek + MCP + Zod。

链路分三层：

1. **理解层**：运营用自然语言描述活动（折扣/签到/抽奖等），System Prompt + JSON Schema 约束输出；动态注入游戏 registry、素材 catalog，相当于轻量 RAG，避免模型凭空编素材 ID。
2. **校验层**：DeepSeek JSON mode 出结构化结果后，服务端用 Zod 再校验一遍——**不过 Schema 不进下游**；素材 ID 做白名单校验，防止幻觉写入。
3. **表现层**：理解层和表现层职责分离，后端 Orchestrator 编排 MCP 工具链（如墨刀 `gen_html` → `import_html`），生成预览再发布。

不可靠时的兜底我主要做三件事：Schema 硬拦截、针对 theme/colors 等易混淆字段加负向约束和 Correction Prompt、失败可回退到人工改结构化配置再继续渲染。  
落到教育场景也一样：比如「自然语言生成试卷/作业规则/课程活动页」，核心仍是 **结构化契约 + 校验闸门 + 工具编排**，而不是裸调一次 LLM。

---

### Q2：Prompt Engineering 你具体怎么设计？有没有踩过坑？

**参考回答：**

我把 Prompt 当成「可版本管理的产品模块」，不是写在业务代码里的一长串字符串。

设计上通常分块：

- **角色与边界**：你是活动配置助手，只输出约定字段，不编造素材。
- **输出契约**：明确 JSON Schema / 字段含义 / 枚举值。
- **动态上下文**：按当前游戏/素材库注入 catalog，减少幻觉。
- **负向约束**：例如 theme 和 colors 易混，明确「theme 是风格标签、colors 是色值数组，禁止互相填充」。
- **纠错闭环**：校验失败时把错误信息回灌给 Correction Prompt，而不是让用户重说一遍。

踩过的坑：早期只靠「请输出 JSON」，模型偶发夹杂解释文字或字段漂移；后来改成 JSON mode + Zod，稳定性明显上去。另一个坑是上下文塞太满，反而干扰主任务——后来只注入当前需要的 registry 子集。  
教育产品里同理：题型、年级、知识点要做成枚举与白名单，Prompt 负责理解意图，**业务规则仍由服务端说了算**。

---

### Q3：什么是 MCP？你在项目里怎么用？和普通 Function Calling 有什么区别？

**参考回答：**

MCP（Model Context Protocol）可以理解为「给 Agent 接外部工具/资源的标准协议」。模型不再只能聊天，而是通过约定好的 tools 去调用外部能力。

我在活动 Agent 里用 MCP 接墨刀的 `gen_html`、以及 Stitch UI 生成能力：理解层先产出结构化配置，再由 Orchestrator 调 MCP 工具生成页面，而不是让 LLM 直接吐一整页不可控 HTML。

和普通 Function Calling 的对比：

| 维度 | Function Calling | MCP |
|------|------------------|-----|
| 形态 | 应用内定义函数给模型调 | 协议化工具/资源，可跨产品复用 |
| 边界 | 多绑定在某一厂商 SDK | 更偏「工具生态」接入 |
| 工程价值 | 适合单应用内编排 | 适合多工具链、可替换 Provider |

我工程上还做了 Provider 切换（modao / stitch），Prompt 模板独立维护，方便换模型或换 UI 生成后端。教育场景也可以把「题库检索、课件模板、作文批改服务」封装成 MCP/工具，让 Agent 编排而不是写死在 Prompt 里。

---

### Q4：如何避免 LLM 幻觉写入业务数据？教育场景你会怎么做？

**参考回答：**

原则是：**模型只提建议，权威数据以系统为准**。

我落地过的手段：

1. Structured Output（JSON mode）锁定结构；
2. Zod / Schema 服务端二次校验；
3. 素材 ID、活动类型等做白名单；
4. 关键写库前走人工确认或灰度发布（预览页 iframe 先看再发布）；
5. 审计日志：记录原始自然语言、模型输出、校验结果、最终入库快照。

教育产品更敏感：学情数据、成绩、题库答案不能被模型「编」进库。我会把「生成练习题 / 解析教案」做成草稿态，教师确认后才正式生效；知识点、教材版本从题库服务查，不允许模型自由 invent ID。  
一句话：**LLM 负责理解与生成草稿，数据库与权限系统负责真相**。

---

### Q5：如果让你把 AI 能力接到教育产品（作业/答疑/内容生产），你会怎么设计一期 MVP？

**参考回答：**

结合我做过的「配置 Agent」经验，一期我会选一个闭环短、可量化的场景，例如：**教师自然语言描述 → 生成一份结构化作业/练习配置 → 预览 → 发布到班级**。

MVP 架构：

- 前端：Vue3 向导（描述 / 预览 / 发布），降低运营/教师学习成本；
- 后端：Node/Java 均可，Orchestrator + Prompt 模块 + Schema 校验；
- 模型：先 DeepSeek/国产模型控成本，预留 Provider 切换；
- 数据：题型、知识点从已有题库检索（RAG），生成结果存草稿表；
- 指标：配置耗时、一次通过率、教师修改率、发布成功率。

不做的事：一期不上「全自动批改入成绩」、不上多 Agent 大而全。先把「人机协同配置效率」跑通，再扩答疑、个性化练习。这和我简历里从「配置化活动引擎」演进到「AI 自动化配置」是同一条产品化路径。

---

## 二、后端相关（5 题）

### Q1：你主导的后台「日均百万级请求」，架构和优化大致怎么做的？

**参考回答：**

业务上是游戏 SDK / 社区 / 商城相关管理与配置后台，Vue3 + Node.js（Express），对接 MySQL，支撑多游戏多业务线。

百万级更多来自 C 端活动/商城与 SDK 侧读多写少的流量特征。我侧重点：

1. **接口分层**：管理后台写操作与 C 端读接口隔离，避免运营批量配置拖垮读链路；
2. **缓存**：热点配置、活动 Schema、商品信息走 Redis/本地缓存，变更时主动失效；
3. **数据库**：活动/商品表按业务键索引，避免大表全扫；读写注意分页与字段裁剪；
4. **稳定性**：超时、限流、错误码规范；关键路径监控；
5. **配置化**：活动引擎标准化后，减少为每个活动改代码发版。

教育产品高峰往往在课前课后、考试周，同样需要「配置读多、管理写少」的隔离和缓存策略。

---

### Q2：REST API 设计你怎么保证可维护？参数校验怎么做？

**参考回答：**

我习惯：

- 资源化路由：`/activities`、`/products`、`/orders`，动作用 HTTP Method 表达；
- 统一响应结构：`code / message / data`，错误可追踪；
- 版本或模块前缀，方便多业务线共存；
- **入口校验**：Express 侧用 Zod（或类似 schema）校验 body/query，非法请求直接 400，不进业务；
- 鉴权与 RBAC：菜单/按钮/接口权限分离，动态路由只解决前端可见性，后端仍要鉴权。

在 AI 项目里，Zod 不只校验人机接口，也校验 LLM 输出——同一套「契约思维」贯穿前后端。若团队是 Java/Spring Boot，对应就是 Bean Validation + DTO，思想一致。

---

### Q3：MySQL 里活动/商品这类表你会怎么设计？和「配置化活动引擎」怎么配合？

**参考回答：**

我会把「稳定字段」和「易变配置」拆开：

- 主表：活动 ID、类型、状态、起止时间、所属游戏/业务线、创建人等；
- 配置体：JSON（或关联配置表）存规则——折扣比例、签到天数、奖池等；
- 商品关联表：活动与商品/SKU 多对多，避免配置里硬编码无法检索的结构；
- 状态机：草稿 → 待发布 → 进行中 → 已结束，发布才对 C 端可见。

配置化引擎的意义是：后台用 Schema 驱动表单渲染，运营改配置不发版；AI Agent 只是「自动填这份 Schema」，最终仍写入同一套表。  
教育侧可类比：作业主表 + 题目关联 + 规则 JSON（限时、允许重做次数），AI 生成的是规则草稿，入库结构不变。

---

### Q4：SSE 流式响应用在什么场景？和 WebSocket 怎么选？

**参考回答：**

我在 AI 相关接口里用过 SSE：适合服务端单向把 LLM token 流式推给前端，实现打字机效果，实现简单，走 HTTP，代理友好。

选型：

- **SSE**：单向、请求-响应生命周期内推流（生成内容、进度日志）；
- **WebSocket**：双向实时（IM、协作编辑、多人课堂互动）。

教育答疑/教案生成用 SSE 通常够用；在线教室互动再用 WS。注意：Node 侧要处理好连接超时、客户端断开取消上游 LLM 请求，避免浪费配额。

---

### Q5：岗位也提了 Java/Spring、Python，你主栈是 Node，怎么快速融入？

**参考回答：**

我主栈是 Node.js（Express）+ MySQL，同时具备 Python 落地和全栈产品化经验。后端共性我很熟：REST、鉴权、事务与一致性、缓存、消息与异步、Schema 校验、可观测性。

融入方式：

1. 先摸清现有教育业务的领域模型（课程/班级/作业/权限）；
2. Java 项目按分层（Controller-Service-Mapper）对标我在 Express 里的路由-服务-DAO；
3. AI 部分继续用 Python 或 Node 做独立服务也可以，通过 HTTP/消息与主站解耦——这和我把 Agent Orchestrator 独立出来是同一思路；
4. 短期能交付的是：**接口设计、配置化后台、AI 接入与校验**，语言不是瓶颈。

我会明确表达：可以按团队主栈贡献，同时把已验证的 AI 工程化经验带过来。

---

## 三、Vue 与小程序相关（5 题）

### Q1：Vue3 管理后台你怎么搭？Pinia、动态路由、权限怎么做？

**参考回答：**

我从 0 到 1 搭过 SDK/社区/商城管理后台：Vue3 + Pinia + Element Plus。

典型做法：

- 布局：侧边栏 + 多页签/内容区；业务按模块拆路由；
- Pinia：用户信息、权限码、字典/全局配置；
- 动态路由：登录后拉菜单权限，`addRoute` 注入；前端路由守卫校验登录态与角色；
- RBAC：角色 → 菜单/按钮权限；按钮用指令或组件控制显隐，**接口仍以后端鉴权为准**；
- 工程化：近期用 Cursor Rules/Skills 统一 Vue3/TS 规范，配合 Vibe Coding 提升后台迭代效率约 30%。

教育后台常见「学校/年级/班级」数据权限，我会在请求拦截器带上组织维度，列表接口做数据范围过滤，不只做菜单权限。

---

### Q2：Vue3 里 Composition API 相比 Options 有什么优势？你怎么组织复杂页面？

**参考回答：**

Composition API 更适合按「业务能力」复用，而不是按 data/methods 拆散。复杂页我会：

- `useXxx` 抽逻辑（列表查询、分页、表单校验、发布流程）；
- 模板保持薄，状态来源清晰；
- 与 TypeScript 结合更好，AI 辅助生成代码时也更稳。

例如活动配置三步向导：`useActivityParse`（调 AI）、`usePreview`（iframe）、`usePublish`（入库），步骤组件只负责编排。  
小程序/H5 里同样可把「用户积分、优惠券领取」抽成 composable，多端复用思想一致。

---

### Q3：你做过小程序（uni-app 等），和 Vue H5 开发有哪些关键差异？

**参考回答：**

我做过优惠券小程序（uni-app），也做过社区的小程序端，和入职小程序维护。

差异重点：

1. **运行环境**：无 DOM/BOM，用小程序组件与 API；注意 `setData`/更新性能，避免一次塞太大对象；
2. **包体积与分包**：主包控制启动，活动/商城走分包；
3. **登录与鉴权**：微信 code → 后端换 session；教育场景还可能绑学校账号，要做账号体系打通；
4. **能力限制**：支付、分享、订阅消息要按平台规范；审核与类目限制要提前评估；
5. **多端**：uni-app 条件编译处理微信/H5 差异；我后来在社区用 Flutter 统一 App/多端，是另一条降低维护成本的路。

答面试时强调：我能独立交付小程序业务闭环，也理解何时用 uni-app、何时用原生/Flutter。

---

### Q4：小程序/H5 列表与表单性能、体验上你会注意什么？

**参考回答：**

- 列表：分页/虚拟列表（长列表）、图片懒加载、骨架屏；
- 防抖节流：搜索、上拉加载；
- 表单：本地校验 + 提交中 loading，避免重复提交（优惠券领取、下单很关键）；
- 状态回显：支付/领取结果页要可重试、可查询，不能只依赖回调一次；
- 弱网：超时提示、缓存关键配置。

社区和商城我做过发帖评论、积分、优惠券、充值等，这些体验细节直接影响转化；教育小程序里「交作业、抢答、课表」同样适用。

---

### Q5：如果教育产品要做「教师端 Vue 后台 + 学生端小程序」，你会怎么划分前后端职责？

**参考回答：**

我会按角色拆端、按领域合后端：

- **教师/运营 Web（Vue3）**：课程编排、作业配置、AI 辅助出题/活动页、数据看板；适合复杂表单与权限；
- **学生/家长小程序**：查课、交作业、消息、轻量互动；适合触达与留存；
- **BFF/API**：统一账号与权限；配置类读接口可缓存；AI 能力独立服务，Web 调生成，小程序只消费已发布内容；
- **发布流**：Web 侧预览确认 → 发布 → 小程序拉已发布版本，避免把未校验的 AI 草稿直接给学生。

这和我做的「后台配置 → C 端 H5/小程序消费」模式一致，只是领域从游戏活动换成教学活动。我可以把活动 Schema、发布流程、AI 填配置的经验直接迁移过去。

---

## 四、面试临场补充（短答备忘）

| 可能追问 | 一句话锚点 |
|---------|------------|
| 为什么看教育岗？ | 配置化工具 + AI 提效路径可迁移；想把 Agent 工程化用到教学内容生产。 |
| AI 会不会替代开发？ | 替代的是重复配表与样板代码；复杂业务、校验、权限、稳定性仍靠工程。 |
| 最大优势？ | 7 年全栈 + 真实 AI Agent 闭环落地，不是只调过 Chat API。 |
| 薪资/到岗？ | 按市场与对方带宽谈；可尽快到岗（简历已标离职）。 |

---

## 五、建议你主动讲的 2 个 STAR 故事

1. **AI 活动配置 Agent**：自然语言 → Schema → MCP 生成 H5，Zod 防幻觉，打通闭环。  
2. **心游社区商城**：全栈支撑三年，创收近 1000 万、转化 25%；后台 Vue3+Node 支撑高并发配置与运营。

把这两个故事练到 90 秒内讲完，再按上面三类题扩展细节即可。
